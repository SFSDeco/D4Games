/*function Item(name, status) {
    this.name = name;
    this.status = status;
  }
  
  // Erzeuge Instanzen der verschiedenen Items
  var doublePointsItem = new Item('doublePoints', false);
  var triplePointsItem = new Item('triplePoints', false);
  var fiveTimesPointsItem = new Item('fiveTimesPoints', false);
  
  // Exportiere die Item-Instanzen, um sie in anderen Dateien zu verwenden
  module.exports = {
    doublePointsItem,
    triplePointsItem,
    fiveTimesPointsItem
  };*/
